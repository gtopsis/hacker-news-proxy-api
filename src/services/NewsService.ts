import { NewsType } from "../types/interfaces";

const getPopularNews = () => {};
const getRecentNews = () => {};

const getNews = (newsType: NewsType) => {
  return [];
};

const getHighlightNew = () => {
  return {};
};

const refreshNews = () => {
  return;
};

export { getNews, getHighlightNew, refreshNews };
